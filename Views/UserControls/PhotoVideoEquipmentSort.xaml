<UserControl x:Class="StoreCatalogWPF.Views.UserControls.PhotoVideoEquipmentSort"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StoreCatalogWPF.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <StackPanel>
            <TextBlock Text="TypeMatrix"/>
            <ComboBox Text="{Binding TypeMatrix}"/>
            <TextBlock Text="MatrixSize"/>
            <ComboBox Text="{Binding MatrixSize }"/>
            <TextBlock Text="Resolution"/>
            <ComboBox Text="{Binding Resolution}"/>
        </StackPanel>
        </Grid>
</UserControl>
